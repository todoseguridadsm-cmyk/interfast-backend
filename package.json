{
  "name": "interfast-monolith",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm install && cd frontend && npm install && npm run build && cd ../backend && npm install",
    "start": "cd backend && npm start"
  }
}
